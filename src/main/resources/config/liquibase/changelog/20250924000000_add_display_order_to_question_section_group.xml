<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog-ext.xsd">

    <!--
        Add display_order column to question_section_group table.
    -->
    <changeSet id="20250924000000-1" author="jhipster">
        <addColumn tableName="question_section_group">
            <column name="display_order" type="int">
                <constraints nullable="true" />
            </column>
        </addColumn>
    </changeSet>

    <!--
        Update existing records to set display_order based on current id order.
    -->
    <changeSet id="20250924000000-2" author="jhipster">
        <sql>
            UPDATE question_section_group 
            SET display_order = (
                SELECT ROW_NUMBER() OVER (ORDER BY id) 
                FROM (SELECT id FROM question_section_group) AS t 
                WHERE t.id = question_section_group.id
            )
        </sql>
    </changeSet>

</databaseChangeLog>
